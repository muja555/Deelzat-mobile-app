import {StyleSheet} from "react-native";

const style = {
    container: {
        justifyContent: 'flex-end',
        margin: 0,
    }
};

const authRequiredModalStyle = StyleSheet.create(style);
export { authRequiredModalStyle as authRequiredModalStyle };

import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
    },
};

const productSubmitModalStyle = StyleSheet.create(style);
export { productSubmitModalStyle as productSubmitModalStyle };

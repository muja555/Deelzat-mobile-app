import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
        backgroundColor: 'black',
    }
};

const productUpdateContainerStyle = StyleSheet.create(style);
export { productUpdateContainerStyle as productUpdateContainerStyle };

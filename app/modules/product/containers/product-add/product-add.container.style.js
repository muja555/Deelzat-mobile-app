import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
        backgroundColor: 'black',
    }
};

const productAddContainerStyle = StyleSheet.create(style);
export { productAddContainerStyle as productAddContainerStyle };

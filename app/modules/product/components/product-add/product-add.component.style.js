import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
        backgroundColor: '#fff'
    },
};

const productAddStyle = StyleSheet.create(style);
export { productAddStyle as productAddStyle };

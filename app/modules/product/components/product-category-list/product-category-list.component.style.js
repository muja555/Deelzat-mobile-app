import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
    },
};

const productCategoryListStyle = StyleSheet.create(style);
export { productCategoryListStyle as productCategoryListStyle };

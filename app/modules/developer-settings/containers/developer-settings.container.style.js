import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
        backgroundColor: 'white'
    }
};

const developerSettingsContainerStyle = StyleSheet.create(style);
export { developerSettingsContainerStyle as developerSettingsContainerStyle };

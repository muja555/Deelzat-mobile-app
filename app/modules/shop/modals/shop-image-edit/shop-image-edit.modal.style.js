import { StyleSheet } from "react-native";

const style = {
    container: {
        justifyContent: 'flex-end',
        margin: 0,
    }
};

const shopImageEditModalStyle = StyleSheet.create(style);
export { shopImageEditModalStyle as shopImageEditModalStyle };

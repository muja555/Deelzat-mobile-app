import {StyleSheet} from "react-native";

const style = {
    container: {
        justifyContent: 'flex-end',
        margin: 0,
    }
};

const fillChatNameModalStyle = StyleSheet.create(style);
export { fillChatNameModalStyle as fillChatNameModalStyle };

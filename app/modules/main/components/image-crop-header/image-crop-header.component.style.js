import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1
    },
};

const imageCropHeaderStyle = StyleSheet.create(style);
export { imageCropHeaderStyle as imageCropHeaderStyle };
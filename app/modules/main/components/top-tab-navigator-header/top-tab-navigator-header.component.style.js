import { StyleSheet } from "react-native";

const style = {
    container: {
        backgroundColor: 'white',
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        zIndex: 1
    }
};

const topTabNavigatorHeaderStyle = StyleSheet.create(style);
export { topTabNavigatorHeaderStyle as topTabNavigatorHeaderStyle };

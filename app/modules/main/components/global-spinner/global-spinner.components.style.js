import { StyleSheet } from "react-native";
import React from "react";

const style = {
    container: {
        width: '110%',
        height: '110%',
        zIndex: 1000,
        backgroundColor: 'white'
    }
}

const globalSpinnerStyle = StyleSheet.create(style);
export { globalSpinnerStyle as globalSpinnerStyle };

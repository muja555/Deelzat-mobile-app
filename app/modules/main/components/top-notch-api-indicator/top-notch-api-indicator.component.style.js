import { StyleSheet } from "react-native";

const style = {
    container: {
        position: 'absolute',
        zIndex: 200,
        height: 5,
        width: '100%',
        backgroundColor: 'red',
    },
};

const topNotchApiIndicatorStyle = StyleSheet.create(style);
export { topNotchApiIndicatorStyle as topNotchApiIndicatorStyle };

import {StyleSheet} from "react-native";

const style = {
    container: {
        justifyContent: 'flex-end',
        margin: 0,
    }
};

const alertModalStyle = StyleSheet.create(style);
export { alertModalStyle as alertModalStyle };

import {StyleSheet} from "react-native";

const style = {
    container: {
        justifyContent: 'flex-end',
        margin: 0,
    }
};

const confirmModalStyle = StyleSheet.create(style);
export { confirmModalStyle as confirmModalStyle };

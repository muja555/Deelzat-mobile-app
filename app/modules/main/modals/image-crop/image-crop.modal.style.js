import {StyleSheet} from "react-native";

const style = {
    container: {
        flex: 1,
        margin: 0,
        backgroundColor: '#fff'
    },
};

const imageCropModalStyle = StyleSheet.create(style);
export { imageCropModalStyle as imageCropModalStyle };

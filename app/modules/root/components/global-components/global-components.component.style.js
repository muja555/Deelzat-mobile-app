import { StyleSheet } from "react-native";

const style = {
    container: {
    },
};

const globalComponentsStyle = StyleSheet.create(style);
export { globalComponentsStyle as globalComponentsStyle };

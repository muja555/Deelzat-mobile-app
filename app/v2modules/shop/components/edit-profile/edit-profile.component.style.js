import { StyleSheet } from "react-native";
import { Colors } from 'deelzat/style';

const style = {
    container: {
        flex: 1
    },
    suggestionsTitle: {
        color: Colors.MAIN_COLOR,
        fontSize: 12,
    },
};

const editProfileStyle = StyleSheet.create(style);
export { editProfileStyle as editProfileStyle };

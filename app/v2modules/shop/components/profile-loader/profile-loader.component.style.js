import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },
};

const profileLoaderStyle = StyleSheet.create(style);
export { profileLoaderStyle as profileLoaderStyle };

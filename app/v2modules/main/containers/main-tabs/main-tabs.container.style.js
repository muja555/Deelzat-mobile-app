import { StyleSheet } from "react-native";

const style = {
    container: {
        flex: 1
    }
};

const mainTabsContainerStyle = StyleSheet.create(style);
export { mainTabsContainerStyle as mainTabsContainerStyle };
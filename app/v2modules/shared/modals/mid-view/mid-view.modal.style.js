import { StyleSheet } from "react-native";

const style = {
    container: {

    },
    content: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 16,
        borderRadius: 8
    },
};

const midViewModalStyle = StyleSheet.create(style);
export { midViewModalStyle as buttonActionModalStyle };

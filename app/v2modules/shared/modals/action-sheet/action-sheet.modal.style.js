import { StyleSheet } from "react-native";

const style = {
    container: {
        justifyContent: 'flex-end',
        margin: 0,
    },
    content: {
        width: '100%'
    },
};

const actionSheetModalStyle = StyleSheet.create(style);
export { actionSheetModalStyle as actionSheetModalStyle };

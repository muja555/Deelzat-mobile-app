import { StyleSheet } from "react-native";

const style = {
    container: {

    },
    shadow: {
        width: 32,
        height: 32,
        opacity: 0.7,
    }
};

const bookmarkButtonStyle = StyleSheet.create(style);
export { bookmarkButtonStyle as bookmarkButtonStyle };
